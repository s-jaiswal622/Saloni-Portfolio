import React from "react";

const Footer = () => {

  return (
    <footer className="text-center text-gray-400 text-sm py-6 border-t border-white/10">
    © {new Date().getFullYear()} Saloni Jaiswal — Built with React, Tailwind CSS & Firebase.
  </footer>
  
  
  );
};

export default Footer;
